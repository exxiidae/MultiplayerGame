export const MODELS_CONFIG = {
    url : [
        '../../models/scout.glb',   // 0 — Scout
        '../../models/assault.glb',     // 1 — Assault
        '../../models/lazership.glb',  // 2
        '../../models/asteroid2.glb', // 3
        '../../models/asteroid.glb'  // 4 — LazerShip (добавь свой файл)
    ]
}