export const SCENE_CONFIG = {
    background: 0x0a0a0a,
    fog: {
        // activity
        enable: true,
        // color scene
        color: 0x0a0a0a,
        // opacity scene back
        density: 0.006
    }
}